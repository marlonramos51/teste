# == Class: class_name
#

class teste {
  	# resources

  file { 'namealteradoaadd':
    ensure => file,
    mode   => '0644',
  }

package { 'todos aa de  sdfsffffffuma vez':
  ensure => installed,
}
}
